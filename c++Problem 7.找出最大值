#include <iostream>

int main() {
    int n, x, max;
    std::cin >> n;
    max = -101;  // 假設輸入數都不小於 -100
    for (int i = 0; i < n; i++) {
        std::cin >> x;
        if (x > max) {
            max = x;
        }
    }
    std::cout << max << std::endl;
    return 0;
}
