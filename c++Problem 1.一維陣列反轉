#include <iostream>
using namespace std;

int main() {
    int number;
    int a[100];

    cin >> number;
    for (int i = 0; i < number; i++) {
        cin >> a[i];
    }

    for (int i = number - 1; i >= 0; i--) {
        cout << a[i];
        if (i > 0) {
            cout << " ";
        }
    }
    cout << endl;

    return 0;
}
