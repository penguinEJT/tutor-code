#include <stdio.h>

int main() {
    int n, x, max;
    scanf("%d", &n);
    max = -101; 
    for (int i = 0; i < n; i++) {
        scanf("%d", &x);
        if (x > max) {
            max = x;
        }
    }
    printf("%d\n", max);
    return 0;
}
