#include <stdio.h>
#include <ctype.h>
int main() {
    char ch;
    scanf("%c", &ch);
    // 將字元轉換為小寫以便統一判斷
    ch = tolower(ch);
    if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
        printf("母音\n");
    } 
    else { 
        printf("子音\n");
    }
    return 0;
}
