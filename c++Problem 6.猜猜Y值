#include <iostream>

int main() {
    int n, x, y;
    std::cin >> n;

    for (int i = 0; i < n; i++) {
        std::cin >> x;
        if (x >= 50 && x <= 70) {
            y = x;
        } else {
            y = 100;
        }
        std::cout << y << std::endl;
    }

    return 0;
}
